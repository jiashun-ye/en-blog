<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Thorough Analysis | Deep Label Distribution Learning: Distributional Supervision and Practical Construction</title>
    <url>/en-blog/2025/10/07/Thorough-Analysis-Deep-Label-Distribution-Learning-Distributional-Supervision-and-Practical-Construction/</url>
    <content><![CDATA[<p>This post is a close reading of Xin Geng et al.’s papers “Deep Label
Distribution Learning for Apparent Age Estimation” (2015) and “Practical
Age Estimation Using Deep Label Distribution Learning” (2020). Starting
from the ICCV Workshops 2015 solution, it walks through the
ChaLearn-facing two-stream CNN, the soft labels generated from the
annotated mean and variance with KL-based distribution supervision, and
the full training/inference pipeline. It then moves to Practical DLDL
(2020), which shrinks the “global” label distribution into a
neighborhood-truncated distribution centered at the true age and
systematically sweeps σ.</p>
<span id="more"></span>
<p><em>Paper links:</em></p>
<p><em><a
href="https://openaccess.thecvf.com/content_iccv_2015_workshops/w11/papers/Yang_Deep_Label_Distribution_ICCV_2015_paper.pdf">Deep
Label Distribution Learning for Apparent Age Estimation</a></em></p>
<p><em><a
href="https://link.springer.com/article/10.1007/s11704-020-8272-4">Practical
Age Estimation Using Deep Label Distribution Learning</a></em></p>
<p><em>Prior article link: <a
href="https://jiashun-ye.github.io/en-blog/2025/03/16/Thorough-Analysis-From%20Single-Label%20to%20Label%20Distribution%20A%20New%20Approach%20to%20Facial%20Age%20Estimation/#more">Thorough
Analysis | From Single Labels to Label Distributions: A New Approach for
Facial Age Estimation</a></em></p>
<h1 id="deep-label-distribution-learning">Deep Label Distribution
Learning</h1>
<h2 id="distribution-supervision">Distribution supervision</h2>
<p>This section corresponds to the ICCV Workshops 2015 paper Deep Label
Distribution Learning for Apparent Age Estimation, which describes the
team’s solution to the ChaLearn Looking at People apparent age
challenge. The dataset is not labeled with biological age as a single
label. Instead, each image has an apparent age mean (m) and standard
deviation () aggregated from multiple annotators to represent
uncertainty. The authors set the learning target to an age distribution
and reported a test score of 0.3057. The organizers provided 3,615
images for training/validation (2,479/1,136); the held-out test set was
used for evaluation only.</p>
<p>Before entering the model, images undergo a unified preprocessing
pipeline: detect faces with DPM, then align with five landmarks (centers
of the eyes, nose tip, and mouth corners) via an affine transform to
produce standardized faces fed to the two network streams. This reduces
pose/alignment variance and stabilizes distribution supervision.</p>
<p>The overall design uses two parallel deep CNN streams:</p>
<ul>
<li><p>Stream 1 (VGG-16 backbone). Input: aligned (224) faces.
Architecture: five conv blocks plus two FC layers. The final layer
outputs 85 logits covering ages 0–84. Training uses either KL divergence
to a soft age distribution or a single-age softmax loss. The schedule
first fine-tunes on MORPH, then performs two separate fine-tunings on
two in-house datasets, and finally fine-tunes on the challenge’s
train+val images. Because the last stage mixes distribution and
classification supervision and the second stage uses two datasets, this
yields four complementary models. At inference, each model’s 85-D output
is concatenated into a 340-D feature. An exponential-kernel
distance-weighted voting maps this feature to a single predicted age,
serving as Stream-1’s output.</p></li>
<li><p>Stream 2 (custom CNN). Input: aligned (256) faces. The first
layer uses an (11) convolution with batch normalization; the head also
outputs 85 logits and is trained with KL loss to match the soft
distribution synthesized from (m,). This stream is pre-trained on
collected face data and fine-tuned on the challenge’s train+val images.
To improve robustness, the authors train six variants with different
input representations and apply heavy test-time augmentation (50 random
scales and flips per image). Averaging 300 predictions (6 reps × 50 TTA)
gives Stream-2’s output.</p></li>
<li><p>Late fusion. Fuse the two scalar age predictions: if their
absolute difference is () years, take the mean; otherwise, keep
Stream-1’s result as final. Test images follow the same detect–align
pipeline, the two streams infer independently, and the fusion rule
produces the submission. The overall flow is shown below.</p></li>
</ul>
<p><img src="DLDL-Flowchart.png" /></p>
<h2 id="practical-formulation">Practical formulation</h2>
<p>In conventional label distribution learning (LDL), a single-age label
is expanded into a global soft distribution over the age space
(Y={1,2,,85}) (often Gaussian or triangular). A Gaussian label centered
at the true age (a) is <span class="math display">\[
D_a(y)=\frac{1}{Z}\exp\!\Big(-\frac{(y-a)^2}{2\sigma^2}\Big),\quad y\in
Y,
\]</span> where (Z) is the partition function ensuring (_{yY}D_a(y)=1):
<span class="math display">\[
Z=\sum_{y\in Y}\exp\!\Big(-\frac{(y-a)^2}{2\sigma^2}\Big).
\]</span></p>
<p>Since ages far from (a) are largely irrelevant, one can truncate the
distribution to a local neighborhood, keeping only ([a-5,a+5]) and
renormalizing: <span class="math display">\[
D_a^{\text{trunc}}(y)=\frac{1}{Z&#39;}\exp\!\Big(-\frac{(y-a)^2}{2\sigma^2}\Big)\,\mathbf{1}\{|y-a|\le
5\},\quad y\in Y,
\]</span> with (Z') enforcing (_{yY}D_a^{}(y)=1): <span
class="math display">\[
Z&#39;=\sum_{\substack{y\in Y\\ |y-a|\le
5}}\exp\!\Big(-\frac{(y-a)^2}{2\sigma^2}\Big).
\]</span> When (a) is near the boundary, the actual neighborhood is
([a-5,a+5]Y).</p>
<p>The CNN produces (D(yx)) (85-D after softmax). Training minimizes KL
divergence (i.e., soft-label cross-entropy) <span
class="math display">\[
\sum_{y\in Y} D_a^{(\cdot)}(y)\log\!\frac{D_a^{(\cdot)}(y)}{\hat D(y\mid
x)},
\]</span> where (D_a^{()}) can be the untruncated or truncated target
distribution.</p>
<h1 id="experiments-and-results">Experiments and results</h1>
<h2 id="chalearn-challenge">ChaLearn challenge</h2>
<p>The paper adopts ChaLearn’s apparent-age score (= 1 - !(- (t-m)^2 /
(2^2))) for evaluation, and reports both validation and test-set
results. On validation, the two streams achieve 0.3534 and 0.3610
respectively; with the simple fusion rule (“use the mean if (|S_1-S_2|),
else use (S_1)”), the score improves to 0.3377. On the final test set,
the method reaches 0.3057, outperforming the organizer-reported human
level (~0.34) and placing in the top 5 that year. Overall, distribution
supervision plus two-stream modeling and light fusion yields consistent,
quantifiable gains—both from single stream → fusion on validation and in
absolute test performance.</p>
<h2 id="truncated-distributions">Truncated distributions</h2>
<p>For the truncated-distribution construction, experiments are run on
MORPH and FG-NET with a consistent “detect → align → resize”
preprocessing: DPM for face detection; five landmarks (eye centers, nose
tip, mouth corners) for geometric alignment; final resolution (224).
This pipeline is illustrated and discussed in the paper.</p>
<p>Training/evaluation uses an 80/20 random split per dataset. The
metric is MAE, with definitions and implementation details provided.
Optimization hyperparameters include an initial LR of 0.001, a total of
80 epochs, mini-batch size 80 for MORPH and 2 for FG-NET, and dropout
0.8. To test the effectiveness of “truncate to a neighborhood,” the
Gaussian shape is kept within ([a-5,a+5]) and () is swept over ({0, 0.5,
1.0, , 5.0}). Triangular labels are discussed for conceptual comparison;
main reports use Gaussians.</p>
<p>The ()–MAE curves show: MORPH is best at () with MAE 2.15; FG-NET is
best at () with MAE 3.14. Both the curve trends and the optima indicate
that “neighborhood truncation + reasonable width” improves
performance.</p>
<p>Against prior methods, the approach achieves 2.15 / 3.14 MAE on MORPH
/ FG-NET, improving over DLDL’s 2.43 / 3.76. Across various () settings,
truncated-Gaussian distribution supervision generally outperforms DLDL
and classic baselines (IIS-LLD, CPNN, ALDL, AGES, etc.). The paper also
reports MORPH errors stratified by gender and race to discuss how data
scale and multi-domain training may affect generalization.</p>
]]></content>
      <categories>
        <category>Thorough Analysis</category>
      </categories>
      <tags>
        <tag>Label Distribution Learning</tag>
      </tags>
  </entry>
  <entry>
    <title>Thorough Analysis | End-to-End Label Distribution Learning: Breaking Fixed Functional-Form Constraints</title>
    <url>/en-blog/2025/09/11/Thorough-Analysis-End-to-End-Label-Distribution-Learning-Breaking-Fixed-Functional-Form-Constraints/</url>
    <content><![CDATA[<p>This post presents a thorough reading of the 2013 paper <em>Facial
Age Estimation by Learning from Label Distributions</em> by Xin Geng et
al. The work extends the 2011 label distribution learning (LDL)
framework and the IIS-LLD optimization method by proposing the
Conditional Probability Neural Network (CPNN), which directly models the
conditional distribution in an end-to-end manner and breaks the
limitation of fixed functional-form assumptions. This article focuses on
the CPNN modeling pipeline and summarizes its experimental results on
the FG-NET and MORPH datasets.</p>
<span id="more"></span>
<p><em>Paper link: <a
href="https://ieeexplore.ieee.org/document/6475129">Facial Age
Estimation by Learning from Label Distributions</a></em></p>
<p><em>Previous post: <a
href="https://jiashun-ye.github.io/en-blog/2025/03/16/Accuracy%20Analysis-From%20Single-Label%20to%20Label%20Distribution%20A%20New%20Approach%20to%20Facial%20Age%20Estimation/">Thorough
Analysis | From Single-Label to Label Distribution: A New Approach to
Facial Age Estimation</a></em></p>
<p>Based on the maximum-entropy model, IIS-LLD needs to pre-specify an
exponential-family form for the conditional probability distribution;
this assumption has limitations and can lead to model misspecification.
To overcome this, the paper proposes CPNN, which models (p(yx))
end-to-end with neural networks, avoids any specific functional-form
assumptions, and leverages label-distribution supervision to achieve
more flexible and accurate age estimation.</p>
<h1 id="flexible-conditional-probability-modeling">Flexible Conditional
Probability Modeling</h1>
<p>The goal is to directly learn the conditional distribution (p(yx)).
Let the input feature be (x^q), the discrete age set (Y={y_1,,y_c}), and
the given training-time label distribution be (D_x(y)). Unlike the
maximum-entropy model (p(yx;)!(,(x,y))), a three-layer perceptron
directly parameterizes (p(yx)) to improve nonlinear fitting capacity and
avoid model misspecification. This is the CPNN algorithm.</p>
<p>A straightforward design would feed (x^q) and output a length-(c) age
distribution in one shot, but that introduces (O(H!!c)) parameters in
the last layer (with (H) the number of hidden units) and is hard to
train robustly with limited data. Instead, the age (y) is also treated
as input. Using a shared-parameter function <span
class="math display">\[
f_\theta(x,y)=\mathrm{MLP}_\theta\!\big([\;x,\
\mathrm{enc}(y)\;]\big)\in\mathbb{R},
\]</span> we enumerate all candidate ages (yY) for the same image to
obtain ({f_(x,y)}_{yY}), and then apply exponential normalization to get
<span class="math display">\[
p_\theta(y\mid
x)=\frac{\exp\!\big(f_\theta(x,y)\big)}{\sum_{y&#39;\in\mathcal
Y}\exp\!\big(f_\theta(x,y&#39;)\big)}.
\]</span> This “(y)-as-input, single scalar output” design preserves the
softmax normalization while markedly reducing parameter count and
naturally sharing parameters across neighboring ages.</p>
<p>The training objective is the distribution-level cross-entropy <span
class="math display">\[
\mathcal L(\theta)=-\sum_{y\in\mathcal Y} D_x(y)\,\log p_\theta(y\mid
x),\qquad
p_\theta(y\mid
x)=\frac{\exp(f_\theta(x,y))}{\sum_{y&#39;}\exp(f_\theta(x,y&#39;))}.
\]</span> Its top-layer gradient is (L/f_(x,y)=p_(yx)-D_x(y)). In
backpropagation, the output layer is linear,
(f_(x,y)=<em>{m=1}<sup>{M_2}<em>{31m},z^{(2)}<em>m(x,y)), where
(z^{(2)}<em>m=G(I</em>{2m})) is the (m)-th hidden unit output,
(I</em>{2m}=</em>{k=0}</sup>{M_1}</em>{2mk},u_k) is its net input,
(u=[1,x,(y)]) concatenates the bias, features, and age encoding, (G) is
sigmoid with derivative (G'). With (<em>{31m}) the hidden-to-output
weights and (</em>{2mk}) the input-to-hidden weights, we have <span
class="math display">\[
\frac{\partial \mathcal L}{\partial \theta_{31m}}
=\sum_{y\in\mathcal Y}\big(p_\theta(y\mid
x)-D_x(y)\big)\,z^{(2)}_m(x,y),\qquad
\]</span> <span class="math display">\[
\frac{\partial \mathcal L}{\partial \theta_{2mk}}
=\sum_{y\in\mathcal Y}\big(p_\theta(y\mid
x)-D_x(y)\big)\,\theta_{31m}\,G&#39;(I_{2m}(x,y))\,u_k(x,y),
\]</span> and in practice standard BP suffices for training.</p>
<h1 id="experiments-and-results">Experiments and Results</h1>
<p>FG-NET uses LOPO validation with MAE; the images come from 82
subjects with 1002 images in total, ages 0–69. MORPH uses 10-fold
cross-validation and reports MAE±std, ages 16–77. The 200-dimensional
features are produced by different pipelines: FG-NET uses the Appearance
Model to extract joint shape and grayscale features and takes the first
200 model parameters; MORPH uses Bio-Inspired Features (BIF) and then
applies Marginal Fisher Analysis (MFA) to reduce to 200 dimensions,
enabling a fair comparison at the same dimensionality.</p>
<p>As this article focuses on CPNN, we only report its performance. The
network is a three-layer perceptron with 400 hidden units; age (y) is
concatenated with image features as input, softmax produces (p(yx)), and
the model is trained with distribution-level cross-entropy. Label
distributions include three ablations: Gaussian, Triangle, and Single
(degenerate), to quantify the “soft supervision” from neighboring ages.
Compared with IIS-LLD under the same features and protocols, CPNN
achieves lower average error but is more data-dependent with slightly
higher variance, while IIS-LLD is more stable and faster due to its
maximum-entropy shape prior and coordinate-wise incremental updates; in
data-reduction experiments, IIS-LLD exhibits a smoother curve. Focusing
on CPNN’s ablations, setting Gaussian () and Triangle base (l) too
narrow (approaching Single) weakens neighboring supervision, while too
wide dilutes the primary label; a moderate width is best (empirically
(!!2), (l!!6)). Here, “more pronounced gains in the elderly sparse
range” means that relative to age ranges with abundant young samples,
and relative to baselines such as Single and AGES, the relative
improvement in elderly ranges (e.g., 60–69) is larger.</p>

<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<style>
  table { width: 76%; margin: 14px auto; border-collapse: collapse; font-size: 0.94em; }
  thead th { border-top: 3px solid #000; border-bottom: 2px solid #000; padding: 4px 6px; text-align: center; background: #f6f6f6; }
  tbody td { padding: 2px 6px; text-align: left; border-bottom: 1px solid #ddd; }
  tbody tr:last-child td { border-bottom: 3px solid #000; }
  caption { font-size: 1.12em; margin: 4px 0 2px; }
</style>
</head>
<body>
  <table>
    <caption>CPNN vs. IIS-LLD: Pros and Cons</caption>
    <thead>
      <tr>
        <th>Method</th>
        <th>Pros</th>
        <th>Limitations</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>IIS-LLD</td>
        <td>Maximum-entropy shape prior brings stability; coordinate-wise incremental updates converge faster; smoother curves when training data is reduced</td>
        <td>Pre-specified \(p(y|x)\) shape may mismatch the task; parameters are learned per label, under-utilizing cross-label correlations</td>
      </tr>
      <tr>
        <td>CPNN</td>
        <td>No assumed shape for \(p(y|x)\), more flexible expression; all labels share parameters, explicitly exploiting neighboring-age correlation; lower average error</td>
        <td>More data-dependent with slightly higher variance; end-to-end training is slower and needs regularization plus moderate distribution width to curb overfitting</td>
      </tr>
    </tbody>
  </table>
</body>
</html>

<p>Causally, CPNN directly models (p(y|x)) and shares parameters across
all labels, injecting the “neighboring ages are similar” structure into
the network via soft supervision. With a proper distribution width, it
achieves lower MAE when data is relatively sufficient or features are
strong; the higher flexibility also makes it more sensitive to data
scale, with slightly higher variance. IIS-LLD fixes (p(y|x)) to an
exponential-family form under maximum entropy and optimizes a lower
bound via coordinate-wise increments, yielding more controllable updates
and better computational efficiency; thus, it often suits scenarios with
limited data that demand robustness and efficiency. However, this shape
constraint limits absorption of cross-label correlations, so in
large-data or strong-feature settings, its average error typically
trails CPNN. In practice: if you aim for peak accuracy and have the
resources, prefer CPNN with appropriate ()/(l) and regularization; if
you prioritize training/inference efficiency or face limited data,
prefer IIS-LLD.</p>
]]></content>
      <categories>
        <category>Thorough Analysis</category>
      </categories>
      <tags>
        <tag>Label Distribution Learning</tag>
      </tags>
  </entry>
  <entry>
    <title>Thorough Analysis | From Unified Variance to Age-Specific Variance: Adaptive Label Distributions and Joint Optimization</title>
    <url>/en-blog/2025/09/17/Thorough-Analysis-From-Unified-Variance-to-Age-Specific-Variance-Adaptive-Label-Distributions-and-Joint-Optimization/</url>
    <content><![CDATA[<p>This article presents a close reading of Xin Geng et al.’s paper
<em>Facial Age Estimation by Adaptive Label Distribution
Learning</em>(2014), systematically reviewing the method framework and
providing key mathematical derivations. It shows the alternating
optimization procedure of adaptive label distributions and age-specific
variance with soft-label construction; the quasi-Newton optimization for
updating the parameters of the conditional probability function; and the
experimental setup and conclusions.</p>
<span id="more"></span>
<p><em>Paper link: <a
href="https://ieeexplore.ieee.org/document/6977477">Facial Age
Estimation by Adaptive Label Distribution Learning</a></em></p>
<p><em>Prior article link: <a
href="https://jiashun-ye.github.io/en-blog/2025/03/16/Thorough-Analysis-From%20Single-Label%20to%20Label%20Distribution%20A%20New%20Approach%20to%20Facial%20Age%20Estimation/#more">Thorough
Analysis | From Single Labels to Label Distributions: A New Approach for
Facial Age Estimation</a></em></p>
<p>Facial aging speed is not constant: children and the elderly change
faster, while young and middle-aged adults change more slowly. The
degree to which neighboring ages can “borrow information” should vary
across age ranges. Label distribution learning with a fixed variance
cannot capture this difference, so ALDL lets each age <span
class="math inline">\(a\)</span> learn its own variance <span
class="math inline">\(\sigma_\alpha\)</span> from data: narrow for
fast-changing ranges, wide for slow-changing ones. The diffusion radius
of soft labels becomes age-specific, fitting the age structure more
finely and improving overall prediction, especially in high-age sparse
regions.</p>
<h1 id="adaptive-label-distribution">Adaptive Label Distribution</h1>
<p>Traditional label distribution learning uses the same variance for
all ages, which conflicts with real aging rhythms: appearance changes
faster in childhood and old age, and more slowly in young to middle age.
Therefore, the same “neighbor borrowing” strength should not be uniform.
Adaptive Label Distribution introduces age-specific variance: for each
age <span class="math inline">\(\alpha\)</span>, it learns the width of
its label distribution—sharper for fast-changing ages, smoother for
slow-changing ones. Let <span
class="math inline">\(\mathcal{Y}=\{y_1,\dots,y_c\}\)</span>. For a
sample <span class="math inline">\(x\)</span> with true age <span
class="math inline">\(\alpha\)</span>, we model its soft label with a
discrete Gaussian: <span class="math display">\[
d_{y,x}(\sigma_\alpha)\;=\;\frac{\exp\!\big(-\tfrac{(y-\alpha)^2}{2\sigma_\alpha^2}\big)}{\sum_{y&#39;\in\mathcal{Y}}\exp\!\big(-\tfrac{(y&#39;-\alpha)^2}{2\sigma_\alpha^2}\big)}\,,
\]</span> where <span class="math inline">\(\sigma_\alpha\)</span> is
the distribution width (age-specific variance) for age <span
class="math inline">\(\alpha\)</span>.</p>
<h2
id="age-specific-variance-estimation-and-soft-label-reconstruction">Age-specific
Variance Estimation and Soft-label Reconstruction</h2>
<p>The entire method is organized as an improved alternating scheme:
start with a unified initial variance <span
class="math inline">\(\sigma_0\)</span> to convert the training set from
single labels to soft labels; then update the conditional-probability
parameters <span class="math inline">\(\theta\)</span> using IIS; next,
select reliable samples under the current model (e.g., those with
absolute error <span class="math inline">\(e_i=\lvert
\alpha_i-\hat\alpha_i\rvert\)</span> not exceeding the round’s training
<span class="math inline">\(\mathrm{MAE}\)</span>), group them by
chronological age, and update the variance <span
class="math inline">\(\sigma_\alpha\)</span> for each age <span
class="math inline">\(\alpha\)</span> using only reliable samples
belonging to <span class="math inline">\(\alpha\)</span>. The cycle
“learn <span class="math inline">\(\theta\)</span> — adjust <span
class="math inline">\(\sigma\)</span> — reconstruct soft labels” repeats
until the error converges.</p>
<p>To avoid notational ambiguity, we stipulate: <span
class="math inline">\(p(y\mid x;\theta)\)</span> denotes the model’s
conditional distribution depending only on <span
class="math inline">\(\theta\)</span>; <span
class="math inline">\(d_{y,\alpha}(\sigma)\)</span> denotes the
normalized probability of a discrete Gaussian template over <span
class="math inline">\(\mathcal{Y}\)</span> with mean <span
class="math inline">\(\alpha\)</span> and standard deviation <span
class="math inline">\(\sigma\)</span>, satisfying <span
class="math inline">\(\sum_{y\in\mathcal{Y}}d_{y,\alpha}(\sigma)=1\)</span>,
with the explicit form <span class="math display">\[
d_{y,\alpha}(\sigma)\;=\;\frac{\exp\!\big(-\tfrac{(y-\alpha)^2}{2\sigma^2}\big)}{\sum_{y&#39;\in\mathcal{Y}}\exp\!\big(-\tfrac{(y&#39;-\alpha)^2}{2\sigma^2}\big)}\,.
\]</span></p>
<p>Formally, given the <span class="math inline">\((k\!-\!1)\)</span>-th
round soft labels <span
class="math inline">\(D_i^{(k-1)}=\big(d_{y,x_i}^{(k-1)}\big)_{y\in\mathcal{Y}}\)</span>,
first perform the standard label-distribution learning update for <span
class="math inline">\(\theta\)</span>: <span class="math display">\[
\theta^{(k)} = \arg\max_{\theta}\sum_i \sum_{y\in\mathcal{Y}}
d_{y,x_i}^{(k-1)} \log p(y\mid x_i;\theta)
\]</span></p>
<p><span class="math display">\[
p(y\mid x;\theta) = \frac{\exp\!\Big(\sum_{r}
\theta_{y,r}\,g_r(x)\Big)}{\sum_{y&#39;}\exp\!\Big(\sum_{r}
\theta_{y&#39;,r}\,g_r(x)\Big)}
\]</span></p>
<p>where <span class="math inline">\(g_r(x)\)</span> is the <span
class="math inline">\(r\)</span>-th feature component of sample <span
class="math inline">\(x\)</span>.</p>
<p>Using <span class="math inline">\(\theta^{(k)}\)</span>, obtain point
predictions <span class="math inline">\(\hat\alpha_i=\arg\max_y p(y\mid
x_i;\theta^{(k)})\)</span> and filter reliable samples. Then, for each
age <span class="math inline">\(\alpha\)</span>, minimize the KL
divergence between the Gaussian template and the model distribution on
its reliable set <span class="math inline">\(I_\alpha\)</span> to update
the variance: <span class="math display">\[
\sigma_\alpha^{(k)} \;=\; \arg\min_{\sigma&gt;0}\;\sum_{m\in
I_\alpha}\sum_{y\in\mathcal{Y}}
d_{y,\alpha}(\sigma)\,\log\frac{d_{y,\alpha}(\sigma)}{\,p(y\mid
x_m;\theta^{(k)})\,}.
\]</span></p>
<p>The log-barrier method incorporates the constraint “<span
class="math inline">\(\sigma&gt;0\)</span>” into the objective, yielding
an unconstrained problem defined on <span
class="math inline">\(\sigma&gt;0\)</span>. Let <span
class="math display">\[
F_\alpha(\sigma)\;=\;\sum_{m\in I_\alpha}\sum_{y\in\mathcal{Y}}
d_{y,\alpha}(\sigma)\,\log\frac{d_{y,\alpha}(\sigma)}{\,p(y\mid
x_m;\theta^{(k)})\,},
\]</span> then its barrier form is <span class="math display">\[
\tilde
F_{\alpha,\mu}(\sigma)\;=\;F_\alpha(\sigma)\;-\;\mu\,\log\sigma,\qquad
\mu&gt;0,\ \sigma&gt;0,
\]</span> and the original constrained optimum is approached by
gradually decreasing <span class="math inline">\(\mu\)</span>. The first
and second derivatives of the resulting 1D objective are <span
class="math display">\[
\frac{\mathrm{d}\tilde F_{\alpha,\mu}}{\mathrm{d}\sigma}
\;=\;\frac{\mathrm{d}F_\alpha}{\mathrm{d}\sigma}\;-\;\frac{\mu}{\sigma},\qquad
\frac{\mathrm{d}^2\tilde F_{\alpha,\mu}}{\mathrm{d}\sigma^2}
\;=\;\frac{\mathrm{d}^2F_\alpha}{\mathrm{d}\sigma^2}\;+\;\frac{\mu}{\sigma^2}.
\]</span></p>
<p>For numerical implementation, write <span
class="math inline">\(F_\alpha(\sigma)\)</span> as a smooth 1D
objective. Its first derivative can be expressed using <span
class="math inline">\(q_y(\sigma)=d_{y,\alpha}(\sigma)\)</span> and its
log-derivative: <span class="math display">\[
\frac{\mathrm{d}F_\alpha}{\mathrm{d}\sigma}
\;=\;\sum_{y\in\mathcal{Y}} \Big(\sum_{m\in I_\alpha}\big[\log
q_y(\sigma)-\log p(y\mid
x_m;\theta^{(k)})+1\big]\Big)\,\frac{\mathrm{d}q_y(\sigma)}{\mathrm{d}\sigma},
\]</span> <span class="math display">\[
\frac{\mathrm{d}q_y(\sigma)}{\mathrm{d}\sigma}
\;=\;q_y(\sigma)\,\frac{(y-\alpha)^2-\mathbb{E}_{q(\sigma)}[(Y-\alpha)^2]}{\sigma^3},\qquad
\mathbb{E}_{q(\sigma)}[(Y-\alpha)^2]=\sum_{y}q_y(\sigma)\,(y-\alpha)^2.
\]</span></p>
<p>After adding the barrier term, the 1D optimization <span
class="math inline">\(\min_{\sigma&gt;0}\tilde
F_{\alpha,\mu}(\sigma)\)</span> can be solved by Newton’s method with
backtracking line search, warm-started from the previous <span
class="math inline">\(\sigma_\alpha^{(k-1)}\)</span>; shrink <span
class="math inline">\(\mu\)</span> and repeat until the outer stopping
criterion is met. Quasi-Newton methods or monotone bracketing strategies
can also be used for the 1D search.</p>
<p>With <span class="math inline">\(\{\sigma_\alpha^{(k)}\}\)</span>
updated, propagate them to all samples to reconstruct soft labels <span
class="math display">\[
d_{y,x_i}^{(k)}\;=\;\frac{\exp\!\big(-\tfrac{(y-\alpha_i)^2}{2(\sigma_{\alpha_i}^{(k)})^2}\big)}{\sum_{y&#39;}\exp\!\big(-\tfrac{(y&#39;-\alpha_i)^2}{2(\sigma_{\alpha_i}^{(k)})^2}\big)}\,,
\]</span> and proceed to the next round. Stop when the MAE improvement
between consecutive rounds falls below <span
class="math inline">\(\varepsilon\)</span>. In this way, the <span
class="math inline">\(\theta\)</span>-update follows the standard
maximum-likelihood form of label-distribution learning, while the <span
class="math inline">\(\sigma\)</span>-update lets the data adaptively
decide “how much neighborhood information” each age should borrow. The
ALDL flowchart is shown below.</p>
<p><img src="ALDL.png" /></p>
<h2 id="quasi-newtondriven-parameter-learning">Quasi-Newton–Driven
Parameter Learning</h2>
<p>The training objective for the conditional probability remains the
same: minimize the distribution-level negative log-likelihood. It arises
by rewriting the KL loss between distributions <span
class="math display">\[
\min_{\theta}\ \sum_{i} \mathrm{KL}\!\left(D_i^{(k-1)}\ \Big\|\
p(\cdot\mid x_i;\theta)\right)
\]</span></p>
<p><span class="math display">\[
\mathrm{KL}\!\left(D_i^{(k-1)}\ \Big\|\ p(\cdot\mid x_i;\theta)\right)
=\sum_{j}
d^{\,k-1}_{y_j,x_i}\,\log\frac{d^{\,k-1}_{y_j,x_i}}{\,p(y_j\mid
x_i;\theta)\,}
\]</span></p>
<p>where <span class="math inline">\(i\)</span> indexes samples; <span
class="math inline">\(j\)</span> indexes labels; <span
class="math inline">\(d^{\,k-1}_{y_j,x_i}\)</span> is the <span
class="math inline">\((k\!-\!1)\)</span>-th round soft-label weight of
sample <span class="math inline">\(x_i\)</span> on label <span
class="math inline">\(y_j\)</span>; <span
class="math inline">\(p(y_j\mid x_i;\theta)\)</span> is the model’s
predicted probability that <span class="math inline">\(x_i\)</span>
belongs to label <span class="math inline">\(y_j\)</span> under
parameters <span class="math inline">\(\theta\)</span>.</p>
<p>This is equivalent to minimizing the weighted cross-entropy because
<span class="math display">\[
\sum_i \mathrm{KL}(D_i^{(k-1)}\|p_i)=\underbrace{\sum_{i,j}
d^{\,k-1}_{y_j,x_i}\log d^{\,k-1}_{y_j,x_i}}_{\text{constant w.r.t.
}\theta}\;-\;\sum_{i,j} d^{\,k-1}_{y_j,x_i}\,\log p(y_j\mid x_i;\theta),
\]</span> so dropping the <span
class="math inline">\(\theta\)</span>-independent constant yields the
negative log-likelihood <span class="math display">\[
T(\theta)=\sum_{i}\log\!\sum_{j}\exp\!\Big(\sum_{r}\theta_{y_j,r}\,x_i^{(r)}\Big)\;-\;\sum_{i,j}
d^{\,k-1}_{y_j,x_i}\,\sum_{r}\theta_{y_j,r}\,x_i^{(r)},
\]</span> with <span class="math display">\[
p(y_j\mid
x_i;\theta)=\frac{\exp\!\big(\sum_{r}\theta_{y_j,r}\,x_i^{(r)}\big)}{\sum_{j&#39;}\exp\!\big(\sum_{r}\theta_{y_{j&#39;},r}\,x_i^{(r)}\big)}\,.
\]</span> Its gradient is <span class="math display">\[
\frac{\partial T(\theta)}{\partial \theta_{y_j,r}}
=\sum_{i}p(y_j\mid x_i;\theta)\,x_i^{(r)}-\sum_{i}
d^{\,k-1}_{y_j,x_i}\,x_i^{(r)}.
\]</span></p>
<p>The quasi-Newton iteration follows the standard derivation: for any
smooth <span class="math inline">\(f(\theta)\)</span>, perform a
second-order Taylor expansion at <span
class="math inline">\(\theta_t\)</span> <span class="math display">\[
f(\theta_t+\Delta)\approx f(\theta_t)+\nabla
f(\theta_t)^\top\Delta+\tfrac12\,\Delta^\top H(\theta_t)\Delta,
\]</span> whose minimizer satisfies the Newton equation <span
class="math display">\[
H(\theta_t)\Delta_N=-\nabla f(\theta_t),
\]</span> and update <span class="math display">\[
\theta_{t+1}=\theta_t+\alpha\,\Delta_N,
\]</span> with <span class="math inline">\(\alpha\)</span> chosen by 1D
line search to satisfy the strong Wolfe conditions. Since forming and
inverting <span class="math inline">\(H^{-1}\)</span> is expensive, BFGS
maintains a symmetric positive-definite matrix <span
class="math inline">\(B_t\approx H(\theta_t)^{-1}\)</span> to
approximate the Newton direction: <span class="math display">\[
s_t=-B_t\,\nabla f(\theta_t),\qquad \theta_{t+1}=\theta_t+\alpha_t s_t,
\]</span> <span class="math display">\[
y_t=\nabla f(\theta_{t+1})-\nabla f(\theta_t),\quad
\rho_t=\frac{1}{y_t^\top s_t},
\]</span> <span class="math display">\[
B_{t+1}=(I-\rho_t s_t y_t^\top)B_t(I-\rho_t y_t s_t^\top)+\rho_t s_t
s_t^\top,
\]</span> and <span class="math inline">\(\alpha_t\)</span> is chosen to
satisfy <span class="math display">\[
f(\theta_t+\alpha_t s_t)\le f(\theta_t)+c_1\alpha_t\,\nabla
f(\theta_t)^\top s_t,\qquad
\big|\nabla f(\theta_t+\alpha_t s_t)^\top s_t\big|\le c_2\big|\nabla
f(\theta_t)^\top s_t\big|
\]</span> with <span class="math inline">\(0&lt;c_1&lt;c_2&lt;1\)</span>
for efficiency and stability.</p>
<p>Applied to <span class="math inline">\(T(\theta)\)</span> here: in
the <span class="math inline">\(k\)</span>-th outer round, warm-start
with <span class="math inline">\(\theta_{k,0}=\theta^{(k-1)}\)</span>
and <span class="math inline">\(B_0=I\)</span>. At the <span
class="math inline">\(l\)</span>-th inner iteration, <span
class="math display">\[
s_l=-B_{l-1}\nabla T(\theta_{k,l-1}),\qquad
\theta_{k,l}=\theta_{k,l-1}+\alpha_l s_l,
\]</span> <span class="math display">\[
y_l=\nabla T(\theta_{k,l})-\nabla T(\theta_{k,l-1}),\qquad
\rho_l=\frac{1}{y_l^\top s_l},
\]</span> <span class="math display">\[
B_l=(I-\rho_l s_l y_l^\top)B_{l-1}(I-\rho_l y_l s_l^\top)+\rho_l s_l
s_l^\top,
\]</span> where <span class="math inline">\(\alpha_l\)</span> is
determined by a strong Wolfe line search. Compared with the previous
section, the only change is replacing IIS with the quasi-Newton scheme
“approximate Newton direction + BFGS recursion + strong Wolfe line
search”.</p>
<h1 id="experiments-and-analysis">Experiments and Analysis</h1>
<p>The paper uses two standard datasets and protocols: FG-NET with LOPO
(leave-one-person-out) and MAE evaluation, containing 82 subjects and
1002 images, ages 0–69; and MORPH (Album II) with 10-fold
cross-validation and MAE±std, ages ~16–77. Feature dimensionality is
unified at 200. FG-NET uses an Appearance Model to extract joint shape +
texture features and truncates to 200 dimensions; MORPH uses BIF and
reduces to 200 dimensions via MFA for comparability. Baselines are
grouped along two axes: adaptivity and solver type. Fixed-variance label
distribution learning (LDL) includes IIS-LDL and BFGS-LDL;
adaptive-variance ALDL includes IIS-ALDL and BFGS-ALDL. We also ablate
soft-label shapes: Gaussian, Triangle, and the degenerate Single (no
diffusion, i.e., a single label).</p>
<p>Overall, adaptive variance markedly reduces MAE, with a more
pronounced advantage in age ranges where high-age samples are sparse
(ALDL &gt; LDL). Under the same settings, replacing IIS with a
quasi-Newton inner optimizer yields faster convergence and lower error;
BFGS-ALDL typically achieves both the lowest MAE and the shortest
training time among the four combinations, with most gains occurring in
the first several outer iterations. In terms of distribution shape,
Gaussian is generally slightly better than Triangle, and Single is
clearly worse—consistent with the intuition that neighboring ages should
borrow information but not be overly diluted. The learned age-specific
variances vary with age, showing the trend of “narrow for fast-changing
ranges, wide for slow-changing ranges,” which mechanistically explains
the performance gains.</p>
]]></content>
      <categories>
        <category>Thorough Analysis</category>
      </categories>
      <tags>
        <tag>Label Distribution Learning</tag>
      </tags>
  </entry>
  <entry>
    <title>Thorough Analysis | From Single Labels to Label Distributions: A New Approach for Facial Age Estimation</title>
    <url>/en-blog/2025/03/16/Thorough-Analysis-From%20Single-Label%20to%20Label%20Distribution%20A%20New%20Approach%20to%20Facial%20Age%20Estimation/</url>
    <content><![CDATA[<p>This article offers a thorough analysis of Xin Geng et al.’s paper
<em>Facial Age Estimation by Learning from Label Distributions</em>
(2011), systematically organizing its research framework and deriving
the key mathematical models in detail. The analysis clarifies the
definition of label distributions, examines the label distribution
learning model that uses KL divergence as its objective, derives the
conditional probability function based on the maximum entropy model
along with its optimization algorithm IIS-LLD, and finally summarizes
the paper’s experimental design and results.</p>
<span id="more"></span>
<p><em>Paper link: <a
href="https://cdn.aaai.org/ojs/7657/7657-13-11187-1-2-20201228.pdf">Facial
Age Estimation by Learning from Label Distributions</a></em></p>
<p>Facial age estimation faces the challenge of data sparsity (e.g.,
insufficient samples in high-age groups). Inspired by the continuity of
age changes (adjacent ages having similar features), Xin Geng proposed
the <em>label distribution learning</em> framework: expanding a single
age label into a distribution that covers neighboring ages. By sharing
age features, this alleviates data scarcity and significantly improves
generalization, especially for higher ages.</p>
<h1 id="defining-label-distributions">Defining Label Distributions</h1>
<p>A label distribution is a generalization of single-label and
multi-label learning. Its meaning differs from probability or fuzzy
classification, though it is highly similar in mathematical structure to
probability. The core idea is that each label is a correct description
of the sample to a different degree, and all labels together form a
complete description of the sample. Compared with traditional
single-label learning, this approach better reflects inter-class
correlations and is particularly suitable for tasks with continuous
variables like age estimation.</p>
<p>Let <span class="math inline">\(\alpha\)</span> be the original label
and <span class="math inline">\(y\)</span> a general label. Converting
from a single label to a label distribution follows two basic
principles:</p>
<ul>
<li><p>The original label has the highest degree of description, i.e.,
<span class="math inline">\(P(\alpha)\)</span> should be the maximum
value in the distribution.</p></li>
<li><p>Labels further away from the original label have lower degrees of
description, i.e., <span class="math inline">\(P(y)\)</span> decreases
as <span class="math inline">\(|y - \alpha|\)</span> increases.</p></li>
</ul>
<p>The paper uses the following two label distributions:</p>
<p><img src="Gaussian_and_Triangular_Distribution.png" /></p>
<p><u><em>Gaussian Distribution</em></u>, which smoothly decreases
neighboring label weights via exponential decay, defined as: <span
class="math display">\[
P(y) = \frac{1}{Z} \exp \left(-\frac{(y - \alpha)^2}{2\sigma^2} \right)
\]</span></p>
<p>Here, <span class="math inline">\(Z\)</span> is the normalization
factor and <span class="math inline">\(\sigma\)</span> controls the
spread of the distribution. The Gaussian distribution makes effective
use of neighboring label information and ensures continuity modeling of
age variation.</p>
<p><u><em>Triangle Distribution</em></u>, which uses linear decay and
assigns weight to neighboring labels only within a certain range: <span
class="math display">\[
P(y) = \max \left(0, 1 - \frac{|y - \alpha|}{l} \right)
\]</span></p>
<p>Here, <span class="math inline">\(l\)</span> determines the span of
the distribution. Compared with the Gaussian distribution, the triangle
distribution is more localized, ensuring labels far from the original
label do not influence the model.</p>
<h1 id="label-distribution-model-and-learning-method">Label Distribution
Model and Learning Method</h1>
<p>Below is the basic structure for modeling and optimizing label
distribution learning:</p>
<p><img src="Training_Process.png" /></p>
<p><img src="Prediction_Process.png" /></p>
<h2 id="optimization-objective-kl-divergence">Optimization Objective: KL
Divergence</h2>
Let the input space be $ \mathcal{X} = \mathbb{R}^d $ and the label space be $ \mathcal{Y} = \{ y_1, y_2, \dots, y_c \} $. In traditional classification, each sample $ x_i $ corresponds to a single label, while in label distribution learning, each sample $ x_i $ is associated with a label distribution $ P_i(y) $ that represents the weight over different labels.

The goal is to learn a conditional probability distribution $ p(y|x; \theta) $ that is as close as possible to $ P_i(y) $. We use KL divergence as the criterion:
<p><span class="math display">\[
\theta^* = \arg\min_{\theta} \sum_i \sum_y P_i(y) \log
\frac{P_i(y)}{p(y|x_i; \theta)}
\]</span></p>
<p>Since <span class="math inline">\(P_i(y)\)</span> is constant for the
training samples, minimizing KL divergence is equivalent to
<em>maximizing the log-likelihood (MLE)</em>: <span
class="math display">\[
\theta^* = \arg\max_{\theta} \sum_i \sum_y P_i(y) \log p(y|x_i; \theta)
\]</span> The label distribution learning framework is closely related
to traditional problems when KL divergence is used as the objective:</p>
<ul>
<li>In a single-label classification task, each sample has a single deterministic label, i.e., $ P_i(y) = \delta(y, y_i) $. The objective degenerates to the classic maximum likelihood estimation:</li>
</ul>
<p><span class="math display">\[
\theta^* = \arg\max_{\theta} \sum_i \log p(y_i | x_i; \theta)
\]</span></p>
<ul>
<li>In a multi-label classification task, each sample $ x_i $ is
associated with a label set $ Y_i $, and we assume $ P_i(y) $ is
uniformly distributed over multiple labels. The objective becomes an
entropy-weighted label assignment, turning multi-label samples into
weighted single-label samples and optimizing the MLE objective:</li>
</ul>
<p><span class="math display">\[
\theta^* = \arg\max_{\theta} \sum_i \frac{1}{|Y_i|} \sum_{y \in Y_i}
\log p(y | x_i; \theta)
\]</span></p>
<h2 id="conditional-probability-form-maximum-entropy-model">Conditional
Probability Form: Maximum Entropy Model</h2>
<h3 id="maximum-entropy-model">Maximum Entropy Model</h3>
To determine the form of the conditional probability function, we need to adjust parameters to optimize the KL divergence. The paper uses the maximum entropy model for the conditional probability function, which is itself an optimization problem. The objective is to maximize entropy—under constraints—so that the conditional distribution $ p(y|x; \theta) $ is as uniform as possible, contains the most information, avoids extra assumptions, and thus improves generalization:
$$
\max_{p(y|x; \theta)}  \sum_{x,y} \tilde{p}(x) p(y|x; \theta) \log p(y|x; \theta)
$$

Here, $p(y|x; \theta)$ is the conditional probability to be optimized; $ \tilde{p}(x,y) $ is the empirical joint distribution estimated from training data; $ \tilde{p}(x) $ is the empirical marginal distribution, i.e., $ \tilde{p}(x) = \sum_y \tilde{p}(x,y) $; $ f_k(x,y) $ are feature functions describing relationships between input $ x $ and label $ y $; and $ \theta $ are model parameters that determine the shape of the conditional distribution.
<p>There are two constraints in the maximum entropy model:</p>
<ul>
<li>Expectation constraint on feature functions, requiring the model’s
expectations to match those of the training data:</li>
</ul>
<p><span class="math display">\[
\sum_{x,y} \tilde{p}(x,y) f_k(x,y) = \sum_{x,y} \tilde{p}(x) p(y|x;
\theta) f_k(x,y), \quad \forall k.
\]</span></p>
<ul>
<li>Probability normalization constraint, ensuring $ p(y|x; ) $ is a
valid probability distribution:</li>
</ul>
<p><span class="math display">\[
\sum_y p(y|x; \theta) = 1, \quad \forall x.
\]</span> <em>Feature functions may feel abrupt; think of them this way:
if a feature function represents the number of facial wrinkles, then the
trained model will, in expectation, fit the wrinkle count of the
training samples. In the paper, the feature functions come from a
feature extractor (a black box). Although its values are not directly
interpretable, a good extractor outputs features highly related to the
target (e.g., age), and this constraint lets the model optimally fit
those prediction-related features.</em></p>
<p>This optimization has a closed-form solution: <span
class="math display">\[
p(y|x; \theta) = \frac{1}{Z} \exp\left( \sum_k \theta_k f_k(x,y)
\right),
\]</span></p>
<p>where</p>
<p><span class="math display">\[
Z = \sum_y \exp\left( \sum_k \theta_k f_k(x,y) \right).
\]</span></p>
<h3 id="theoretical-derivation-of-the-closed-form-solution">Theoretical
Derivation of the Closed-Form Solution</h3>
<p>The optimization objective of the maximum entropy model is to
maximize conditional entropy:</p>
<p><span class="math display">\[
H(P) = - \sum_{x,y} \tilde{P}(x) P(y|x) \log P(y|x)
\]</span></p>
<p>subject to the following constraints:</p>
<ul>
<li>Empirical expectation constraint:</li>
</ul>
<p><span class="math display">\[
E_P(f_i) = E_{\tilde{P}}(f_i), \quad i=1,2,\dots,n
\]</span></p>
<ul>
<li>Probability normalization:</li>
</ul>
<p><span class="math display">\[
\sum_y P(y|x) = 1
\]</span></p>
<p>Introduce Lagrange multipliers <span class="math inline">\(w_0, w_1,
\dots, w_n\)</span> to construct the Lagrangian:</p>
<p><span class="math display">\[
L(P,w) = -H(P) + w_0 \left( 1 - \sum_y P(y|x) \right) + \sum_{i=1}^{n}
w_i \left( E_{\tilde{P}}(f_i) - E_P(f_i) \right)
\]</span></p>
<p>Expanding:</p>
<p><span class="math display">\[
L(P, w) = \sum_{x,y} \tilde{P}(x) P(y|x) \log P(y|x) + w_0 \left( 1 -
\sum_y P(y|x) \right)
\]</span></p>
<p><span class="math display">\[
+ \sum_{i=1}^{n} w_i \left( \sum_{x,y} \tilde{P}(x,y) f_i(x,y) -
\sum_{x,y} \tilde{P}(x) P(y|x) f_i(x,y) \right)
\]</span></p>
<p>Taking the partial derivative with respect to <span
class="math inline">\(P(y|x)\)</span>:</p>
<p><span class="math display">\[
\frac{\partial L(P,w)}{\partial P(y|x)} = \tilde{P}(x) \left[ \log
P(y|x) + 1 \right] - \tilde{P}(x) \sum_{i=1}^{n} w_i f_i(x,y) - w_0
\]</span></p>
<p>Setting it to zero:</p>
<p><span class="math display">\[
\log P(y|x) + 1 = \sum_{i=1}^{n} w_i f_i(x,y) + w_0
\]</span></p>
<p>i.e.,</p>
<p><span class="math display">\[
P(y|x) = \exp \left( \sum_{i=1}^{n} w_i f_i(x,y) + w_0 - 1 \right)
\]</span></p>
<p>Since <span class="math inline">\(P(y|x)\)</span> must satisfy
normalization:</p>
<p><span class="math display">\[
\sum_y P(y|x) = 1
\]</span></p>
<p>we normalize:</p>
<p><span class="math display">\[
\sum_y \exp \left( \sum_{i=1}^{n} w_i f_i(x,y) + w_0 - 1 \right) = 1
\]</span></p>
<p>Define the normalization factor:</p>
<p><span class="math display">\[
Z(x) = \sum_y \exp \left( \sum_{i=1}^{n} w_i f_i(x,y) \right)
\]</span></p>
<p>then:</p>
<p><span class="math display">\[
\exp (w_0 - 1) = \frac{1}{Z(x)}
\]</span></p>
<p>Substitute back:</p>
<p><span class="math display">\[
P(y|x) = \frac{\exp \left( \sum_{i=1}^{n} w_i f_i(x,y) \right)}{Z(x)}
\]</span></p>
<p>Thus the closed-form solution of the maximum entropy model is:</p>
<p><span class="math display">\[
P(y|x) = \frac{\exp \left( \sum_{i=1}^{n} w_i f_i(x,y) \right)}{Z(x)}
\]</span></p>
<p>where:</p>
<p><span class="math display">\[
Z(x) = \sum_y \exp \left( \sum_{i=1}^{n} w_i f_i(x,y) \right)
\]</span></p>
<p>This shows the maximum entropy model takes an exponential form—an
exponential family—where <span class="math inline">\(w_i\)</span> are to
be optimized.</p>
<p><em>If you’re careful, you may notice that in the derivation, the
closed-form solution was plugged into the normalization constraint to
determine <span class="math inline">\(w_0\)</span>. Why not also plug it
into the expectation-equality constraint to determine <span
class="math inline">\(w_i\)</span>? Would the expectation equality still
hold? Think of it like this: without the expectation constraint on
feature functions, the final model under maximum entropy would be
uniform. Adding this constraint yields an exponential-form distribution.
The expectation constraint acts as a “data-driven prior,” determining
the distribution’s shape and guiding the model to leverage data
features. It’s a “soft constraint” that fixes the form and forces the
model to match the data’s statistics while allowing some
flexibility.</em></p>
<h2 id="optimization-algorithm-iis-lld">Optimization Algorithm:
IIS-LLD</h2>
<h3 id="objective-function">Objective Function</h3>
<p>Plug the exponential-form conditional probability into the KL
divergence: <span class="math display">\[
T(\theta) = \sum_{i} \sum_{y} P_i(y) \log \left( \frac{1}{Z(x_i)} \exp
\left( \sum_k \theta_{y,k} g_k(x_i) \right) \right)
\]</span> Expand the logarithm: <span class="math display">\[
T(\theta) = \sum_{i} \sum_{y} P_i(y) \sum_k \theta_{y,k} g_k(x_i) -
\sum_i \sum_{y} P_i(y) \log Z(x_i)
\]</span></p>
<p>Since <span class="math inline">\(Z(x_i)\)</span> is independent of
<span class="math inline">\(y\)</span> and <span
class="math inline">\(\sum_y P_i(y) = 1\)</span>: <span
class="math display">\[
\sum_{y} P_i(y) \log Z(x_i) = \log Z(x_i) \sum_y P_i(y) = \log Z(x_i)
\]</span> Thus <span class="math display">\[
T(\theta) = \sum_{i} \sum_{y} P_i(y) \sum_k \theta_{y,k} g_k(x_i) -
\sum_i \log Z(x_i)
\]</span> Expand <span class="math inline">\(Z(x_i)\)</span>: <span
class="math display">\[
T(\theta) = \sum_{i} \sum_{y} P_i(y) \sum_k \theta_{y,k} g_k(x_i) -
\sum_i \log \sum_y \exp \left( \sum_k \theta_{y,k} g_k(x_i) \right)
\]</span></p>
<h3 id="lower-bound-on-the-objective-increment">Lower Bound on the
Objective Increment</h3>
<p>We aim to maximize: <span class="math display">\[
T(\theta) = \sum_{i,y} P_i(y) \sum_k \theta_{y,k} g_k(x_i) - \sum_i \log
Z(x_i)
\]</span> where <span class="math display">\[
Z(x_i) = \sum_y \exp \left( \sum_k \theta_{y,k} g_k(x_i) \right)
\]</span></p>
<p>Define the parameter update: <span class="math display">\[
\theta^{(t+1)} = \theta^{(t)} + \Delta
\]</span> with increments: <span class="math display">\[
\delta_{y,k} = \theta_{y,k}^{(t+1)} - \theta_{y,k}^{(t)}
\]</span></p>
<p>The updated normalization term: <span class="math display">\[
Z^*(x_i) = \sum_y \exp \left( \sum_k (\theta_{y,k} + \delta_{y,k})
g_k(x_i) \right)
\]</span></p>
<p>Objective change: <span class="math display">\[
T(\theta + \Delta) - T(\theta) =
\sum_{i,y} P_i(y) \sum_k \delta_{y,k} g_k(x_i) - \sum_i \log
\frac{Z^*(x_i)}{Z(x_i)}
\]</span></p>
<p>Compute the second term: <span class="math display">\[
\frac{Z^*(x_i)}{Z(x_i)} = \frac{\sum_y \exp \left( \sum_k (\theta_{y,k}
+ \delta_{y,k}) g_k(x_i) \right)}
{\sum_y \exp \left( \sum_k \theta_{y,k} g_k(x_i) \right)}
\]</span></p>
<p>Factor out the original normalization: <span class="math display">\[
\frac{Z^*(x_i)}{Z(x_i)} =
\sum_y \frac{\exp \left( \sum_k \theta_{y,k} g_k(x_i) \right)}{Z(x_i)}
\exp \left( \sum_k \delta_{y,k} g_k(x_i) \right)
\]</span></p>
<p>Since <span class="math display">\[
p(y | x_i; \theta) = \frac{\exp \left( \sum_k \theta_{y,k} g_k(x_i)
\right)}{Z(x_i)}
\]</span></p>
<p>we have <span class="math display">\[
\frac{Z^*(x_i)}{Z(x_i)} = \sum_y p(y | x_i; \theta) \exp \left( \sum_k
\delta_{y,k} g_k(x_i) \right)
\]</span></p>
<p>Using the inequality <span class="math display">\[
-\log x \geq 1 - x
\]</span></p>
<p>and applying it to the objective change: <span
class="math display">\[
-\sum_i \log \sum_y p(y | x_i; \theta) \exp \left( \sum_k \delta_{y,k}
g_k(x_i) \right)
\geq
\sum_i \left( 1 - \sum_y p(y | x_i; \theta) \exp \left( \sum_k
\delta_{y,k} g_k(x_i) \right) \right)
\]</span></p>
<p>Hence <span class="math display">\[
T(\theta + \Delta) - T(\theta) \geq
\sum_{i,y} P_i(y) \sum_k \delta_{y,k} g_k(x_i) + n - \sum_i \sum_y p(y |
x_i; \theta) \exp \left( \sum_k \delta_{y,k} g_k(x_i) \right)
\]</span></p>
<p>Next, apply Jensen’s inequality.</p>
<p>Define <span class="math display">\[
g^\#(x_i) = \sum_k |g_k(x_i)|
\]</span></p>
<p>Since the exponential function is convex, by Jensen’s inequality,
letting $ s(g_k(x_i)) $ be the sign of $ g_k(x_i) $: <span
class="math display">\[
\sum_k \frac{|g_k(x_i)|}{g^\#(x_i)} \exp \left( \delta_{y,k} s(g_k(x_i))
g^\#(x_i) \right)
\geq
\exp \left( \sum_k \frac{|g_k(x_i)|}{g^\#(x_i)} \delta_{y,k} g^\#(x_i)
\right)
\]</span></p>
<p>i.e., <span class="math display">\[
\sum_k \frac{|g_k(x_i)|}{g^\#(x_i)} \exp \left( \delta_{y,k} s(g_k(x_i))
g^\#(x_i) \right)
\geq
\exp \left( \sum_k \delta_{y,k} |g_k(x_i)| \right)
\]</span></p>
<p>Reversing the sides: <span class="math display">\[
-\exp \left( \sum_k \delta_{y,k} |g_k(x_i)| \right)
\geq
-\sum_k \frac{|g_k(x_i)|}{g^\#(x_i)} \exp \left( \delta_{y,k}
s(g_k(x_i)) g^\#(x_i) \right)
\]</span></p>
<p>Substitute into the previous lower bound: <span
class="math display">\[
T(\theta + \Delta) - T(\theta) \geq
\sum_{i,y} P_i(y) \sum_k \delta_{y,k} g_k(x_i) + n - \sum_{i, y} p(y |
x_i; \theta)\sum_k \frac{|g_k(x_i)|}{g^\#(x_i)} \exp \left( \delta_{y,k}
s(g_k(x_i)) g^\#(x_i) \right)
\]</span></p>
<p>Define the right-hand side as: <span class="math display">\[
\mathcal{A}(\Delta | \theta) =
\sum_{i,y} P_i(y) \sum_k \delta_{y,k} g_k(x_i) + n - \sum_{i, y} p(y |
x_i; \theta)\sum_k \frac{|g_k(x_i)|}{g^\#(x_i)} \exp \left( \delta_{y,k}
s(g_k(x_i)) g^\#(x_i) \right)
\]</span></p>
<p>Thus <span class="math display">\[
T(\theta + \Delta) - T(\theta) \geq \mathcal{A}(\Delta | \theta)
\]</span></p>
<h3 id="iis-lld-algorithm">IIS-LLD Algorithm</h3>
<p>Through the Jensen-based relaxation, <span
class="math inline">\(T(\theta)\)</span> is lower-bounded by <span
class="math inline">\(\mathcal{A}(\Delta | \theta)\)</span> such that
the dimensions decouple and can be optimized independently.</p>
<p>We optimize by taking derivatives of <span
class="math inline">\(\mathcal{A}(\Delta | \theta)\)</span> and setting
them to zero, yielding the increment for each dimension: <span
class="math display">\[
\frac{\partial \mathcal{A}(\Delta | \theta)}{\partial \delta_{y,k}} =
\sum_{i} P_i(y) g_k(x_i) - \sum_{i} p(y | x_i; \theta) g_k(x_i)
\exp(\delta_{y,k} s(g_k(x_i)) g^\#(x_i)) = 0.
\]</span></p>
<p>This equation can be solved numerically (e.g., with the Gauss–Newton
method) dimension by dimension to obtain <span
class="math inline">\(\delta_{y,k}\)</span> and complete iterative
updates. The algorithm flow is as follows:</p>
<p><img src="IIS-LLD_Algorithm_Flow_Chart.png" /></p>
<p><em>The classic IIS algorithm requires nonnegative feature functions.
To ensure flexibility of the label distribution model, the paper removes
this restriction, hence the name IIS-LLD. While the final solution can
still be obtained, from a numerical perspective this may introduce
instability.</em></p>
<h1 id="experiments-and-results">Experiments and Results</h1>
<p>The experiments use the <em>FG-NET Aging Database</em>, which
contains 1002 facial images from 82 subjects aged 0–69. Each subject has
6–18 photos, with variations in illumination, expression, and pose. Due
to fewer samples at higher ages, the researchers adopt
<em>Leave-One-Person-Out (LOPO) cross-validation</em>: each round uses
one subject’s images as the test set and the others as the training set,
repeated for 82 rounds to obtain the final result. <em>Mean Absolute
Error (MAE)</em> is used as the error metric between predicted and true
ages.</p>
<p>Feature extraction is based on an <em>Appearance Model</em>. The
model first detects facial landmarks, applies <em>PCA (Principal
Component Analysis)</em> to reduce the dimensionality of landmark
coordinates, then normalizes the face to a canonical shape, performs PCA
on the normalized grayscale image to extract texture features, and
finally combines shape and texture into a single feature vector. During
PCA, 95% of variance is retained, keeping the first 200 principal
components.</p>
<p>The methods and key parameters used in the experiments are listed
below:</p>


<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>三线表</title>
    <style>
        table {
            width: 80%;
            margin: 20px auto;
            border-collapse: collapse;
        }

        table caption {
            font-size: 1.5em;
            margin-bottom: 10px;
        }
    
        table thead > tr > th {
            border-top: 3px solid #000;
            border-bottom: 2px solid #000;
            padding: 4px;
            text-align: center;
            background-color: #f4f4f4;
        }
    
        table tbody > tr > td {
            padding: 2px;
            text-align: center;
            border-bottom: 1px solid #ddd;
        }
    
        table tbody > tr:last-child {
            border-bottom: 3px solid #000;
        }
    
        table td:last-child {
            border-right: none;
        }
    
        table {
            border: none;
        }
    </style>
</head>
<body>
    <table>
        <thead>
            <tr>
                <th>Method</th>
                <th>Key Parameters</th>
            </tr>
        </thead>
        <tbody>
            <tr><td>IIS-LLD (Gaussian)</td><td>Gaussian distribution, standard deviation \( \sigma = 1 \)</td></tr>
            <tr><td>IIS-LLD (Triangle)</td><td>Triangular distribution, base length \( l = 6 \)</td></tr>
            <tr><td>IIS-LLD (Single)</td><td>Traditional single label, only true age used</td></tr>
            <tr><td>AGES</td><td>Aging pattern subspace dimension = 20</td></tr>
            <tr><td>WAS</td><td>No parameter setting required</td></tr>
            <tr><td>AAS</td><td>Error threshold = 3, age groups = (0-9, 10-19, 20-39, 40-69)</td></tr>
            <tr><td>KNN</td><td>Number of nearest neighbors \( K = 30 \)</td></tr>
            <tr><td>BP (Neural Network)</td><td>Hidden layer neurons = 100</td></tr>
            <tr><td>C4.5 (Decision Tree)</td><td>Using J4.8 version, default parameters</td></tr>
            <tr><td>SVM</td><td>Kernel function: RBF, kernel width = 1</td></tr>
            <tr><td>HumanA (Grayscale Image)</td><td>Only shows grayscale face image with background removed</td></tr>
            <tr><td>HumanB (Color Image)</td><td>Shows full color image, including hair, clothing, background, etc.</td></tr>
        </tbody>
    </table>
</body>
</html>


<p>The experimental results are shown below. Bars outperforming HumanA
are highlighted in purple; those outperforming HumanB are marked with a
vertical dashed line:</p>
<p><img src="Age_Estimation_Results.png" /></p>
<p><em>From the figure we can see that even without using label
distributions, the IIS-LLD algorithm performs very well—thanks to the
favorable properties of the maximum entropy model. Label distributions
further enhance performance, pushing it beyond HumanB. The Gaussian
distribution covers all ages, the triangle distribution only nearby
ages, and the single-label case only the age itself—their performance
decreases in that order.</em></p>
<p>The paper also tunes the parameters <span
class="math inline">\(\sigma\)</span> and <span
class="math inline">\(l\)</span> of the two label distributions and
reports the corresponding MAE as follows:</p>
<p><img src="deviation.png" /></p>
<p><em>Larger parameters imply a more dispersed distribution. When both
parameters are zero, both distributions degenerate to a single label.
The best performance arises by balancing the contribution from
neighboring labels so they help without “overpowering” the true
label.</em></p>
<p>Next is a visualization of the MAE across age ranges for the three
IIS-LLD variants and the strongest baseline AGES, where <span
class="math inline">\(n\)</span> denotes the number of samples in that
range.</p>
<p><img src="MAE_in_Different_Age_Ranges.png" /></p>
<p><em>In age ranges with abundant data (younger groups), IIS-LLD is
slightly inferior to AGES, which is specialized for facial age
estimation; in data-sparse older ranges, IIS-LLD clearly outperforms
AGES. This validates the idea in the paper that label distributions
compensate for data scarcity.</em></p>
<p>Finally, the authors outline three scenarios where label
distributions are particularly valuable to address label uncertainty and
data scarcity: when instance labels are inherently distributions; when
strong correlations exist among classes; and when labels from different
sources are disputed.</p>
]]></content>
      <categories>
        <category>Thorough Analysis</category>
      </categories>
      <tags>
        <tag>Label Distribution Learning</tag>
      </tags>
  </entry>
</search>
