<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>JS.Ye’s Research Hub</title>
  
  <subtitle>Paper Reviews, Course Notes, and Research Progress</subtitle>
  <link href="http://jiashun-ye.github.io/en-blog/atom.xml" rel="self"/>
  
  <link href="http://jiashun-ye.github.io/en-blog/"/>
  <updated>2025-04-02T08:13:14.122Z</updated>
  <id>http://jiashun-ye.github.io/en-blog/</id>
  
  <author>
    <name>Jiashun Ye</name>
    
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>Thorough Analysis | From Single-Label to Label Distribution: A New Approach to Facial Age Estimation</title>
    <link href="http://jiashun-ye.github.io/en-blog/2025/03/16/Accuracy%20Analysis-From%20Single-Label%20to%20Label%20Distribution%20A%20New%20Approach%20to%20Facial%20Age%20Estimation/"/>
    <id>http://jiashun-ye.github.io/en-blog/2025/03/16/Accuracy%20Analysis-From%20Single-Label%20to%20Label%20Distribution%20A%20New%20Approach%20to%20Facial%20Age%20Estimation/</id>
    <published>2025-03-16T12:12:50.000Z</published>
    <updated>2025-04-02T08:13:14.122Z</updated>
    
    <content type="html"><![CDATA[<p>This article provides a detailed analysis of the paper <em>Facial AgeEstimation by Learning from Label Distributions</em> by Xin Geng et al.,systematically outlining the research framework and deriving the keymathematical models in detail. The analysis process includes definingthe concept of label distribution, analyzing the label distributionlearning model with the Kullback-Leibler divergence as the objective,deriving the conditional probability function based on the maximumentropy model and its optimization algorithm IIS-LLD, and concludingwith a summary of the experimental design and results presented in thepaper.</p><span id="more"></span><p>Facial age estimation faces challenges of data sparsity (e.g., lackof samples in higher age groups). Inspired by the continuity of ageprogression (similar features in adjacent ages), Xin Geng proposed the<em>Label Distribution Learning (LDL)</em> framework: it expands asingle age label into a distribution covering neighboring ages,mitigating data deficiency through shared age features, andsignificantly improving model generalization, especially the accuracy inolder age groups.</p><h1 id="define-label-distribution">Define Label Distribution</h1><p>Label distribution is a generalization of single-label andmulti-label learning, and its meaning differs from probability or fuzzyclassification, though it is mathematically highly similar toprobability. The core concept of label distribution is that each labelis a correct description of the sample, but to varying degrees, and thecombination of all labels forms a complete description of the sample.Compared to traditional single-label learning, this method betterreflects the correlation between categories, making it particularlysuitable for continuous variable tasks like age estimation.</p><p>Let <span class="math inline">\(\alpha\)</span> be the original labeland <span class="math inline">\(y\)</span> be a general label. Theconversion from single label to label distribution needs to follow twobasic principles:</p><ul><li><p>The original label has the highest description degree, i.e.,<span class="math inline">\(P(\alpha)\)</span> should be the maximumvalue in the distribution.</p></li><li><p>The farther a label is from the original label, the lower itsdescription degree, i.e., <span class="math inline">\(P(y)\)</span>decreases as <span class="math inline">\(|y - \alpha|\)</span>increases.</p></li></ul><p>The label distributions used in the paper are as follows:</p><p><img src="Gaussian_and_Triangular_Distribution.png" /></p><p><u><em>Gaussian Distribution</em></u>, which smoothly reduces theweights of neighboring labels through exponential decay, defined as:<span class="math display">\[P(y) = \frac{1}{Z} \exp \left(-\frac{(y - \alpha)^2}{2\sigma^2} \right)\]</span></p><p>where <span class="math inline">\(Z\)</span> is the normalizationfactor and <span class="math inline">\(\sigma\)</span> controls thespread of the distribution. The Gaussian distribution effectivelyutilizes information from adjacent labels, ensuring the model capturesthe continuity of age changes.</p><p><u><em>Triangle Distribution</em></u>, which assigns weights toneighboring labels within a fixed range using a linear decay: <spanclass="math display">\[P(y) = \max \left(0, 1 - \frac{|y - \alpha|}{l} \right)\]</span></p><p>where <span class="math inline">\(l\)</span> determines the span ofthe distribution. Compared to the Gaussian distribution, the triangledistribution is more localized, ensuring that labels far from theoriginal have no influence on the model.</p><h1 id="label-distribution-model-and-learning-method">Label DistributionModel and Learning Method</h1><p>Below is the basic structure of modeling and optimization methods inlabel distribution learning:</p><p><img src="Training_Process.png" /></p><p><img src="Prediction_Process.png" /></p><h2 id="optimization-objective-kl-divergence">Optimization Objective: KLDivergence</h2>Let the input space be $ \mathcal{X} = \mathbb{R}^d $ and the label space be $ \mathcal{Y} = \{ y_1, y_2, \dots, y_c \} $. In traditional classification tasks, each sample $ x_i $ corresponds to only one label. In label distribution learning, however, each sample $ x_i $ is associated with a label distribution $ P_i(y) $, representing the weight of this sample over different labels.The goal is to learn a conditional probability distribution $ p(y|x; \theta) $ that approximates $ P_i(y) $ as closely as possible. KL divergence is used as the evaluation metric:<p><span class="math display">\[\theta^* = \arg\min_{\theta} \sum_i \sum_y P_i(y) \log\frac{P_i(y)}{p(y|x_i; \theta)}\]</span></p><p>Since <span class="math inline">\(P_i(y)\)</span> is fixed fortraining samples, minimizing KL divergence is equivalent to<em>Maximizing Log-Likelihood (MLE)</em>: <span class="math display">\[\theta^* = \arg\max_{\theta} \sum_i \sum_y P_i(y) \log p(y|x_i; \theta)\]</span></p><p>The label distribution learning framework is closely related totraditional problems when using KL divergence as the objectivefunction:</p><ul><li>In single-label classification tasks, each sample has only one definite label, i.e., $ P_i(y) = \delta(y, y_i) $. The objective function degenerates into the classic maximum likelihood estimation:</li></ul><p><span class="math display">\[\theta^* = \arg\max_{\theta} \sum_i \log p(y_i | x_i; \theta)\]</span></p><ul><li>In multi-label classification tasks, each sample $ x_i $ isassociated with a label set $ Y_i $. Assuming $ P_i(y) $ is uniformlydistributed over multiple labels, the objective function becomes anentropy-weighted label assignment method, transforming multi-labelsamples into weighted single-label samples and optimizing the maximumlikelihood objective:</li></ul><p><span class="math display">\[\theta^* = \arg\max_{\theta} \sum_i \frac{1}{|Y_i|} \sum_{y \in Y_i}\log p(y | x_i; \theta)\]</span></p><h2 id="conditional-probability-form-maximum-entropy-model">ConditionalProbability Form: Maximum Entropy Model</h2><h3 id="maximum-entropy-model">Maximum Entropy Model</h3>To adjust the parameters and optimize KL divergence, the form of the conditional probability function must be determined. The paper uses the maximum entropy model to obtain the conditional probability function, which is also an optimization problem. The objective function is to maximize entropy, i.e., under the given constraints, the conditional distribution $ p(y|x; \theta) $ should be as uniform as possible, containing the most information and avoiding introducing additional assumptions, thereby improving the model's generalization ability.$$\max_{p(y|x; \theta)}  \sum_{x,y} \tilde{p}(x) p(y|x; \theta) \log p(y|x; \theta)$$where $p(y|x; \theta)$ is the conditional probability distribution given input $ x $, which is the target distribution to be optimized; $ \tilde{p}(x,y) $ is the empirical joint distribution estimated from the training data; $ \tilde{p}(x) $ is the empirical marginal distribution, i.e., $ \tilde{p}(x) = \sum_y \tilde{p}(x,y) $; $ f_k(x,y) $ is the feature function, representing some feature between input $ x $ and class $ y $; and $\theta$ is the model parameter that determines the form of the conditional probability distribution.<p>The maximum entropy model has two constraints:</p><ul><li>Expectation constraint of feature functions, requiring the expectedvalue of the model's feature functions to match the training data:</li></ul><p><span class="math display">\[\sum_{x,y} \tilde{p}(x,y) f_k(x,y) = \sum_{x,y} \tilde{p}(x) p(y|x;\theta) f_k(x,y), \quad \forall k.\]</span></p><ul><li>Probability normalization constraint, ensuring that $ p(y|x; ) $ isa valid probability distribution:</li></ul><p><span class="math display">\[\sum_y p(y|x; \theta) = 1, \quad \forall x.\]</span> <em>Feature functions may seem abrupt, but can be understoodthis way: if the feature function represents the number of wrinkles onthe face, the trained model will, in expectation, align as closely aspossible with the wrinkle count in the training samples. In the paper,feature functions are provided by a feature extractor, which is a blackbox. While its numerical values cannot be directly interpreted, a goodfeature extractor outputs features that are highly relevant to thetarget (such as age prediction), so this constraint allows the model tooptimally fit those relevant features.</em></p><p>This optimization problem has an analytical solution: <spanclass="math display">\[p(y|x; \theta) = \frac{1}{Z} \exp\left( \sum_k \theta_k f_k(x,y)\right),\]</span></p><p>where</p><p><span class="math display">\[Z = \sum_y \exp\left( \sum_k \theta_k f_k(x,y) \right).\]</span></p><h3 id="analytical-solution-derivation">Analytical SolutionDerivation</h3><p>The optimization objective of the maximum entropy model is tomaximize conditional entropy:</p><p><span class="math display">\[H(P) = - \sum_{x,y} \tilde{P}(x) P(y|x) \log P(y|x)\]</span></p><p>while satisfying the following constraints:</p><ul><li>Empirical expectation constraint:</li></ul><p><span class="math display">\[E_P(f_i) = E_{\tilde{P}}(f_i), \quad i=1,2,\dots,n\]</span></p><ul><li>Probability normalization condition:</li></ul><p><span class="math display">\[\sum_y P(y|x) = 1\]</span></p><p>To do this, we introduce Lagrange multipliers <spanclass="math inline">\(w_0, w_1, \dots, w_n\)</span> to construct theLagrangian function:</p><p><span class="math display">\[L(P,w) = -H(P) + w_0 \left( 1 - \sum_y P(y|x) \right) + \sum_{i=1}^{n}w_i \left( E_{\tilde{P}}(f_i) - E_P(f_i) \right)\]</span></p><p>Expanding this into a specific form:</p><p><span class="math display">\[L(P, w) = \sum_{x,y} \tilde{P}(x) P(y|x) \log P(y|x) + w_0 \left( 1 -\sum_y P(y|x) \right)\]</span></p><p><span class="math display">\[+ \sum_{i=1}^{n} w_i \left( \sum_{x,y} \tilde{P}(x,y) f_i(x,y) -\sum_{x,y} \tilde{P}(x) P(y|x) f_i(x,y) \right)\]</span></p><p>Taking the partial derivative with respect to <spanclass="math inline">\(P(y|x)\)</span>:</p><p><span class="math display">\[\frac{\partial L(P,w)}{\partial P(y|x)} = \tilde{P}(x) \left[ \logP(y|x) + 1 \right] - \tilde{P}(x) \sum_{i=1}^{n} w_i f_i(x,y) - w_0\]</span></p><p>Setting the partial derivative to zero:</p><p><span class="math display">\[\log P(y|x) + 1 = \sum_{i=1}^{n} w_i f_i(x,y) + w_0\]</span></p><p>Thus:</p><p><span class="math display">\[P(y|x) = \exp \left( \sum_{i=1}^{n} w_i f_i(x,y) + w_0 - 1 \right)\]</span></p><p>Since <span class="math inline">\(P(y|x)\)</span> needs to satisfythe normalization condition:</p><p><span class="math display">\[\sum_y P(y|x) = 1\]</span></p><p>We normalize <span class="math inline">\(P(y|x)\)</span>:</p><p><span class="math display">\[\sum_y \exp \left( \sum_{i=1}^{n} w_i f_i(x,y) + w_0 - 1 \right) = 1\]</span></p><p>Defining the normalization factor:</p><p><span class="math display">\[Z(x) = \sum_y \exp \left( \sum_{i=1}^{n} w_i f_i(x,y) \right)\]</span></p><p>Thus:</p><p><span class="math display">\[\exp (w_0 - 1) = \frac{1}{Z(x)}\]</span></p><p>Substituting into the expression for <spanclass="math inline">\(P(y|x)\)</span>:</p><p><span class="math display">\[P(y|x) = \frac{\exp \left( \sum_{i=1}^{n} w_i f_i(x,y) \right)}{Z(x)}\]</span></p><p>Finally, we obtain the analytical solution for the maximum entropymodel:</p><p><span class="math display">\[P(y|x) = \frac{\exp \left( \sum_{i=1}^{n} w_i f_i(x,y) \right)}{Z(x)}\]</span></p><p>where:</p><p><span class="math display">\[Z(x) = \sum_y \exp \left( \sum_{i=1}^{n} w_i f_i(x,y) \right)\]</span></p><p>This shows that the maximum entropy model has an exponential form,i.e., an exponential family distribution, where <spanclass="math inline">\(w_i\)</span> are the parameters to beoptimized.</p><p><em>If you're attentive, you may notice that during the derivation,the analytical solution is substituted into the probabilitynormalization constraint to determine the parameter <spanclass="math inline">\(w_0\)</span>. Why don't we substitute theexpectation equality constraint to determine the <spanclass="math inline">\(w_i\)</span>? Could we still ensure theexpectation equality? Here's an interpretation: without the featurefunction expectation constraint, the final model would be a uniformdistribution in the maximum entropy sense. When this constraint isadded, it leads to an exponential distribution function. The expectationconstraint serves as a "data-driven prior" that shapes the distribution,guiding the model on how to use data features. It acts as a "softconstraint," defining the distribution's shape and forcing the model tomatch the statistical properties of the data, while allowing someflexibility in adjustment.</em></p><h2 id="optimization-algorithm-iis-lld">Optimization Algorithm:IIS-LLD</h2><h3 id="objective-function">Objective Function</h3><p>Substituting the obtained exponential conditional probabilityfunction into the KL divergence expression results in: <spanclass="math display">\[T(\theta) = \sum_{i} \sum_{y} P_i(y) \log \left( \frac{1}{Z(x_i)} \exp\left( \sum_k \theta_{y,k} g_k(x_i) \right) \right)\]</span> Expanding the logarithm: <span class="math display">\[T(\theta) = \sum_{i} \sum_{y} P_i(y) \sum_k \theta_{y,k} g_k(x_i) -\sum_i \sum_{y} P_i(y) \log Z(x_i)\]</span></p><p>Since <span class="math inline">\(Z(x_i)\)</span> is independent of<span class="math inline">\(y\)</span>, and <spanclass="math inline">\(P_i(y)\)</span> summed over <spanclass="math inline">\(y\)</span> equals 1: <span class="math display">\[\sum_{y} P_i(y) \log Z(x_i) = \log Z(x_i) \sum_y P_i(y) = \log Z(x_i)\]</span> Thus: <span class="math display">\[T(\theta) = \sum_{i} \sum_{y} P_i(y) \sum_k \theta_{y,k} g_k(x_i) -\sum_i \log Z(x_i)\]</span> Expanding <span class="math inline">\(Z(x_i)\)</span>:</p><p><span class="math display">\[T(\theta) = \sum_{i} \sum_{y} P_i(y) \sum_k \theta_{y,k} g_k(x_i) -\sum_i \log \sum_y \exp \left( \sum_k \theta_{y,k} g_k(x_i) \right)\]</span></p><h3 id="objective-function-increment-lower-bound-derivation">ObjectiveFunction Increment Lower Bound Derivation</h3><p>We want to maximize the objective function: <spanclass="math display">\[T(\theta) = \sum_{i,y} P_i(y) \sum_k \theta_{y,k} g_k(x_i) - \sum_i \logZ(x_i)\]</span> where: <span class="math display">\[Z(x_i) = \sum_y \exp \left( \sum_k \theta_{y,k} g_k(x_i) \right)\]</span></p><p>Define the parameter update: <span class="math display">\[\theta^{(t+1)} = \theta^{(t)} + \Delta\]</span> where the increment is: <span class="math display">\[\delta_{y,k} = \theta_{y,k}^{(t+1)} - \theta_{y,k}^{(t)}\]</span></p><p>The normalized factor after the increment: <spanclass="math display">\[Z^*(x_i) = \sum_y \exp \left( \sum_k (\theta_{y,k} + \delta_{y,k})g_k(x_i) \right)\]</span></p><p>The increment of the objective function: <spanclass="math display">\[T(\theta + \Delta) - T(\theta) =\sum_{i,y} P_i(y) \sum_k \delta_{y,k} g_k(x_i) - \sum_i \log\frac{Z^*(x_i)}{Z(x_i)}\]</span></p><p>Computing the latter term: <span class="math display">\[\frac{Z^*(x_i)}{Z(x_i)} = \frac{\sum_y \exp \left( \sum_k (\theta_{y,k}+ \delta_{y,k}) g_k(x_i) \right)}{\sum_y \exp \left( \sum_k \theta_{y,k} g_k(x_i) \right)}\]</span></p><p>Extracting the original normalization term: <spanclass="math display">\[\frac{Z^*(x_i)}{Z(x_i)} =\sum_y \frac{\exp \left( \sum_k \theta_{y,k} g_k(x_i) \right)}{Z(x_i)}\exp \left( \sum_k \delta_{y,k} g_k(x_i) \right)\]</span></p><p>Since: <span class="math display">\[p(y | x_i; \theta) = \frac{\exp \left( \sum_k \theta_{y,k} g_k(x_i)\right)}{Z(x_i)}\]</span></p><p>Thus: <span class="math display">\[\frac{Z^*(x_i)}{Z(x_i)} = \sum_y p(y | x_i; \theta) \exp \left( \sum_k\delta_{y,k} g_k(x_i) \right)\]</span></p><p>Using logarithmic inequalities for scaling: <spanclass="math display">\[-\log x \geq 1 - x\]</span></p><p>Applying this to the objective function increment: <spanclass="math display">\[-\sum_i \log \sum_y p(y | x_i; \theta) \exp \left( \sum_k \delta_{y,k}g_k(x_i) \right)\geq\sum_i \left( 1 - \sum_y p(y | x_i; \theta) \exp \left( \sum_k\delta_{y,k} g_k(x_i) \right) \right)\]</span></p><p>Thus: <span class="math display">\[T(\theta + \Delta) - T(\theta) \geq\sum_{i,y} P_i(y) \sum_k \delta_{y,k} g_k(x_i) + n - \sum_i \sum_y p(y |x_i; \theta) \exp \left( \sum_k \delta_{y,k} g_k(x_i) \right)\]</span></p><p>Next, we continue the scaling using Jensen's inequality:</p><p>Define: <span class="math display">\[g^\#(x_i) = \sum_k |g_k(x_i)|\]</span></p><p>Since the exponential function is convex, according to Jensen'sinequality, let $ s(g_k(x_i)) $ be the sign function of $ g_k(x_i) $:<span class="math display">\[\sum_k \frac{|g_k(x_i)|}{g^\#(x_i)} \exp \left( \delta_{y,k} s(g_k(x_i))g^\#(x_i) \right)\geq\exp \left( \sum_k \frac{|g_k(x_i)|}{g^\#(x_i)} \delta_{y,k} g^\#(x_i)\right)\]</span></p><p>That is: <span class="math display">\[\sum_k \frac{|g_k(x_i)|}{g^\#(x_i)} \exp \left( \delta_{y,k} s(g_k(x_i))g^\#(x_i) \right)\geq\exp \left( \sum_k \delta_{y,k} |g_k(x_i)| \right)\]</span></p><p>Reversing the direction: <span class="math display">\[-\exp \left( \sum_k \delta_{y,k} |g_k(x_i)| \right)\geq-\sum_k \frac{|g_k(x_i)|}{g^\#(x_i)} \exp \left( \delta_{y,k}s(g_k(x_i)) g^\#(x_i) \right)\]</span></p><p>Substitute this back into the previous lower bound: <spanclass="math display">\[T(\theta + \Delta) - T(\theta) \geq\sum_{i,y} P_i(y) \sum_k \delta_{y,k} g_k(x_i) + n - \sum_{i, y} p(y |x_i; \theta)\sum_k \frac{|g_k(x_i)|}{g^\#(x_i)} \exp \left( \delta_{y,k}s(g_k(x_i)) g^\#(x_i) \right)\]</span></p><p>We define the right-hand side expression as: <spanclass="math display">\[\mathcal{A}(\Delta | \theta) =\sum_{i,y} P_i(y) \sum_k \delta_{y,k} g_k(x_i) + n - \sum_{i, y} p(y |x_i; \theta)\sum_k \frac{|g_k(x_i)|}{g^\#(x_i)} \exp \left( \delta_{y,k}s(g_k(x_i)) g^\#(x_i) \right)\]</span></p><p>Thus: <span class="math display">\[T(\theta + \Delta) - T(\theta) \geq \mathcal{A}(\Delta | \theta)\]</span></p><h3 id="iis-lld-algorithm">IIS-LLD Algorithm</h3><p><span class="math inline">\(T(\theta)\)</span>, through scaling viaJensen's inequality, makes <spanclass="math inline">\(\mathcal{A}(\Delta | \theta)\)</span> independentin each dimension, allowing for independent optimization of eachdimension.</p><p>The optimization process is to take the derivative of <spanclass="math inline">\(\mathcal{A}(\Delta | \theta)\)</span> and set thegradient to zero, obtaining the increment for each dimension:</p><p><span class="math display">\[\frac{\partial \mathcal{A}(\Delta | \theta)}{\partial \delta_{y,k}} =\sum_{i} P_i(y) g_k(x_i) - \sum_{i} p(y | x_i; \theta) g_k(x_i)\exp(\delta_{y,k} s(g_k(x_i)) g^\#(x_i)) = 0.\]</span></p><p>This equation can be solved numerically (e.g., using Gauss-Newtonmethod) to find the increment <spanclass="math inline">\(\delta_{y,k}\)</span> for each dimension, andultimately complete the iterative update. The algorithm flow is asfollows:</p><p><img src="IIS-LLD_Algorithm_Flow_Chart.png" /></p><p><em>The classic IIS algorithm requires feature functions to benon-negative. To ensure flexibility in the label distribution model, thepaper removes this restriction, hence the algorithm is named IIS-LLD.From an algorithmic perspective, this still leads to the final solution,but from a numerical method perspective, it may introduce instabilityrisks.</em></p><h1 id="experiments-and-result-analysis">Experiments and ResultAnalysis</h1><p>The experiments were conducted using the <em>FG-NET AgingDatabase</em>, which contains 1002 facial images from 82 subjects, withages ranging from 0 to 69 years. Each subject has between 6 and 18photos, and the dataset includes variations in lighting, facialexpressions, and head poses. Due to the limited number of elderlysamples, the researchers adopted <em>Leave-One-Person-Out (LOPO)cross-validation</em>, in which all photos of one subject are used asthe test set, and the remaining subjects' photos are used for training.This process is repeated 82 times to compute the final result. Theevaluation metric is <em>Mean Absolute Error (MAE)</em>, which measuresthe average absolute error between the predicted and actual ages.</p><p>The feature extraction method is based on the <em>AppearanceModel</em>. The model first detects facial landmarks, then applies<em>PCA (Principal Component Analysis)</em> to reduce the dimensionalityof the landmark coordinates. The face image is then normalized to astandard shape, and PCA is applied again to extract texture featuresfrom the standardized grayscale image. Finally, shape and texturefeatures are combined into a single feature vector. In PCAdimensionality reduction, the researchers retained 95% of the datavariability, selecting the first 200 principal components.</p><p>The following are the methods used in the experiments and theircorresponding key parameters:</p><!DOCTYPE html><html lang="zh-CN"><head>    <meta charset="UTF-8">    <meta name="viewport" content="width=device-width, initial-scale=1.0">    <title>Three-Line Table</title>    <style>        table {            width: 100%;            margin: 20px auto;            border-collapse: collapse;        }        table caption {            font-size: 1.5em;            margin-bottom: 10px;        }            table thead > tr > th {            border-top: 3px solid #000;            border-bottom: 2px solid #000;            padding: 4px;            text-align: center;            background-color: #f4f4f4;        }            table tbody > tr > td {            padding: 2px;            text-align: center;            border-bottom: 1px solid #ddd;        }            table tbody > tr:last-child {            border-bottom: 3px solid #000;        }            table td:last-child {            border-right: none;        }            table {            border: none;        }    </style></head><body>    <table>        <thead>            <tr>                <th>Method</th>                <th>Key Parameters</th>            </tr>        </thead>        <tbody>            <tr><td>IIS-LLD (Gaussian)</td><td>Gaussian distribution, standard deviation \( \sigma = 1 \)</td></tr>            <tr><td>IIS-LLD (Triangle)</td><td>Triangle distribution, base length \( l = 6 \)</td></tr>            <tr><td>IIS-LLD (Single)</td><td>Traditional single label, only true age used</td></tr>            <tr><td>AGES</td><td>Aging pattern subspace dimension = 20</td></tr>            <tr><td>WAS</td><td>No parameter setting required</td></tr>            <tr><td>AAS</td><td>Error threshold = 3, age groups = (0–9, 10–19, 20–39, 40–69)</td></tr>            <tr><td>KNN</td><td>Number of neighbors \( K = 30 \)</td></tr>            <tr><td>BP (Neural Network)</td><td>Hidden layer neurons = 100</td></tr>            <tr><td>C4.5 (Decision Tree)</td><td>Using J4.8 version, default parameters</td></tr>            <tr><td>SVM</td><td>Kernel: RBF, kernel width = 1</td></tr>            <tr><td>HumanA (Grayscale Image)</td><td>Only shows grayscale face with background removed</td></tr>            <tr><td>HumanB (Color Image)</td><td>Shows full color image including hair, clothing, background, etc.</td></tr>        </tbody>    </table></body></html><p>The experimental results are shown below. Purple bars indicate betterperformance than HumanA, and vertical dashed lines indicate betterperformance than HumanB:</p><p><img src="Age_Estimation_Results.png" /></p><p><em>From the figure, we can see that even without using labeldistribution, the IIS-LLD algorithm already performs very well. This isthanks to the good properties of the maximum entropy model. Labeldistribution adds further benefits, enabling the algorithm to outperformHumanB. The Gaussian distribution covers all ages, the triangledistribution only covers neighboring ages, and single label only coversthe exact age — their performance gradually decreases in thatorder.</em></p><p>The paper also adjusted the parameters <spanclass="math inline">\(\sigma\)</span> and <spanclass="math inline">\(l\)</span> for the two types of labeldistributions and tested the corresponding MAE. The results are asfollows:</p><p><img src="deviation.png" /></p><p><em>A larger parameter means a more dispersed distribution. When theparameter is 0 for both distributions, they degenerate into single-labellearning. A balance must be maintained so that nearby labels contributeto the true label without overwhelming it, in order to achieve optimalperformance.</em></p><p>The following shows a visualization of the MAE across different agegroups for the three IIS-LLD algorithms and the best-performing baselinemethod AGES. "n" indicates the number of samples in that age group.</p><p><img src="MAE_in_Different_Age_Ranges.png" /></p><p><em>In age groups with sufficient data (younger ages), the IIS-LLDmodel slightly underperforms the specialized AGES model. However, inolder age groups with sparse data, the IIS-LLD model significantlyoutperforms AGES. This validates the paper’s idea of using labeldistribution to compensate for the lack of data.</em></p><p>Finally, the authors highlight three scenarios where labeldistribution is valuable, especially in addressing label uncertainty anddata scarcity:The initial label of the instance is itself a distributionover classes; There is strong correlation among classes; Labels fromdifferent sources are inconsistent or disputed.</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;This article provides a detailed analysis of the paper &lt;em&gt;Facial Age
Estimation by Learning from Label Distributions&lt;/em&gt; by Xin Geng et al.,
systematically outlining the research framework and deriving the key
mathematical models in detail. The analysis process includes defining
the concept of label distribution, analyzing the label distribution
learning model with the Kullback-Leibler divergence as the objective,
deriving the conditional probability function based on the maximum
entropy model and its optimization algorithm IIS-LLD, and concluding
with a summary of the experimental design and results presented in the
paper.&lt;/p&gt;</summary>
    
    
    
    <category term="Paper Review" scheme="http://jiashun-ye.github.io/en-blog/categories/Paper-Review/"/>
    
    <category term="Thorough Analysis" scheme="http://jiashun-ye.github.io/en-blog/categories/Paper-Review/Thorough-Analysis/"/>
    
    
    <category term="Artificial Intelligence" scheme="http://jiashun-ye.github.io/en-blog/tags/Artificial-Intelligence/"/>
    
    <category term="Machine Learning" scheme="http://jiashun-ye.github.io/en-blog/tags/Machine-Learning/"/>
    
    <category term="Label Distribution Learning" scheme="http://jiashun-ye.github.io/en-blog/tags/Label-Distribution-Learning/"/>
    
  </entry>
  
</feed>
